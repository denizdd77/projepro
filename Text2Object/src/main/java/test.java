/*
public double totalPrice(List<Ticket> tickets){
        double sum=0;
        for(int i=0;i<tickets.size();i++){

        tickets.get(totalPrice(i));
        sum+=totalPrice(i);
        }
        return sum;
        }
public String toString(){ // gerekli mi?
        return"";
        }
public int confirmedPrice(List<Ticket> tickets){
        int sum1=0;
        for(int i=0;i<tickets.size();i++){
        if(status.equals("CONFIRMED")){
        sum1+=totalPrice;
        }
        }
        return sum1;
        }
public int cancelledPrice(List<Ticket> tickets){
        int sum2=0;
        if(status.equals("CANCELLED"))
        {
        }

        for(Ticket t:tickets){

        }
        }
public String Eur_Try_Total(List<Ticket> tickets){
        int sumEUR=0;
        int sumTRY=0;
        }
public int out_in_difference(List<Ticket> tickets){
        int a,b,x1,x2;
        int out_max=Math.max(a,b);
        int in_max=Math.max(x1,x2);
        }
        }


// use comma as seperator

                     /* System.out.println("Parsing [id= " + parsing[0] +
                            " , bookingTime=" + parsing[1] +
                            " , checkIn=" + parsing[2] +
                            " , checkOut=" + parsing[3] +
                            " , currency=" + parsing[4] +
                            " , totalPrice=" + parsing[5] +
                            " , status="  + parsing[6] +  "]");
                            */

